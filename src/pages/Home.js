import React from 'react';
import Carousel from '../componentes/Carousel/Carousel';

const Home = () => {
    return (
        <section className="home-page">
            <h2 className="section-title">✨ Nossas Delícias Fresquinhas de Natal ✨</h2>
            <Carousel />
        </section>
    );
};

export default Home;